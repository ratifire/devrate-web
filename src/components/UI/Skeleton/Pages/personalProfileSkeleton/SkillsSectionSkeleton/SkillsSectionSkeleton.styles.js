export const styles = {
  wrapper: (theme) => ({
    padding: theme.spacing(4),
  }),
  elem: (theme) => ({
    marginBottom: theme.spacing(2),
  }),
  lastElem: {
    marginBottom: '29px',
  },
  box: {
    display: 'flex',
    justifyContent: 'space-between',
  }
};
