export const styles = {
  wrapper: (theme) => ({
    maxWidth: '1110px',
    width: '100%',
    padding: theme.spacing(4),
  }),
  box: {
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
    marginBottom: '20px',
  },
};
