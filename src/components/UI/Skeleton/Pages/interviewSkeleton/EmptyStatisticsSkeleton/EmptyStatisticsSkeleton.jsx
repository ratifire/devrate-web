import { Skeleton } from '@mui/material';

const EmptyStatisticsSkeleton = () => {
  return <Skeleton height={'100%'} variant={'rounded'} width={'100%'} />;
};

export default EmptyStatisticsSkeleton;
