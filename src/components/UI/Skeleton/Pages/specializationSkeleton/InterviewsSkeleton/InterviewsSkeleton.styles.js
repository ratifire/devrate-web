export const styles = {
  wrapper: (theme) => ({
    padding: theme.spacing(4),
    display: 'flex',
    flexDirection: 'column',
    gap: '24px',
  }),
  box: {
    display: 'flex',
    flexDirection: 'column',
    gap: '4px',
  },
};
