export const styles = {
  wrapper: (theme) => ({
    padding: theme.spacing(3),
  }),
  title: {
    marginBottom: '11px',
  },
  box: {
    display: 'flex',
    justifyContent: 'space-between',
    marginBottom: '10px',
  },
};
