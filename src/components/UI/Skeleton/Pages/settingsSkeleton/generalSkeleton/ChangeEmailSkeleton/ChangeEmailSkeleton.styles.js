export const styles = {
  container: (theme) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: theme.spacing(5),
  }),
  box: (theme) => ({
    display: 'flex',
    gap: theme.spacing(3),
  }),
  btn: {
    maxWidth: '228px',
    width: '100%',
  },
};
