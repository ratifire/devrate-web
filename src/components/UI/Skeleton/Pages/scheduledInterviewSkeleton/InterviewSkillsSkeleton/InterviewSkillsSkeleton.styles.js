export const styles = {
  wrapper: (theme) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: '24px',
    padding: theme.spacing(4),
  }),
  box: {
    display: 'flex',
    justifyContent: 'space-between',
    gap: '10px',
  },
};
