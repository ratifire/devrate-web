export const styles = {
  wrapper: (theme) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: '16px',
    padding: theme.spacing(4),
  }),
  box: {
    display: 'flex',
    justifyContent: 'space-between',
    gap: '20px',
  },
};
