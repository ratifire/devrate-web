export const styles = (theme) => ({
  iconButton: {
    color: theme.palette.primary[200],
    position: 'absolute',
    top: '0px',
    right: '10px',
  },
  bookmarkIcon: {
    fontSize: '24px',
  },
});
