export const styles = {
  container: (theme) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: theme.spacing(4),
  }),
  sendBox: (theme) => ({
    display: 'flex',
    gap: theme.spacing(3),
    alignSelf: 'start',
    maxWidth: '472px',
    width: '100%',
  }),
  btn: {
    maxWidth: '228px',
  },
  formBox: (theme) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: theme.spacing(4),
  }),
};
