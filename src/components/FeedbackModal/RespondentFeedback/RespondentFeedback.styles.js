export const styles = {
  title: (theme) => ({
    color: theme.palette.text.primary,
    fontSize: '20px',
    paddingBottom: '25px',
    fontWeight: 500,
  }),
  subtitle: (theme) => ({
    color: theme.palette.text.primary,
    fontSize: '20px',
    fontWeight: 500,
  }),
  btn: {
    marginTop: '28px',
    width: '228px',
  },
};
