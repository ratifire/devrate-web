export const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    gap: '24px',
  },
  title: (theme) => ({
    color: theme.palette.text.primary,
    fontSize: '20px',
    fontWeight: 500,
  }),
  subtitle: (theme) => ({
    color: theme.palette.text.primary,
    fontSize: '20px',
    fontWeight: 500,
  }),
  btn: {
    width: '228px',
  },
};
