export const styles = {
  textHelper: (theme) => ({
    color: theme.palette.modals.inputs.labelColor.error,
    marginTop: '2px',
    fontSize: '12px',
    position: 'absolute',
    bottom: '-21px',
    left: '0px',
  }),
};
