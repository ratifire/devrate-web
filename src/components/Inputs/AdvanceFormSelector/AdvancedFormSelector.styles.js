export const styles = {
  wrapper: (theme) => ({
    width: '100%',
    minWidth: 150,
    marginBottom: theme.spacing(4),
    '& + .Mui-expanded': {},
  }),
  dropdownPaper: {
    maxHeight: '200px',
    backgroundColor: '#1d1d1d',
  },
};