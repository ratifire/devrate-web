export const styles = {
  newsAgreementText: (theme) => ({
    fontWeight: 300,
    fontSize: 14,
    lineHeight: 1.28,
    color: theme.palette.text.primary,
  }),
  checkBox: () => ({
    color: '#F1F1F1',
    '&.Mui-checked': {
      color: '#F1F1F1',
    },
  }),
  textHelper: () => ({
    position: 'absolute',
    bottom: '-23px',
  }),
};
