export const styles = {
  input: {
    width: '100%',
    minWidth: 150,
    marginBottom: '24px',
  },
  textHelper: {
    position: 'absolute',
    bottom: '-23px',
  },
  menuPaper: (theme) => ({
    '& .MuiPaper-root': {
      backgroundColor: theme.palette.background.level2,
    },
  }),
};
