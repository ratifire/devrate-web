export const styles = {
  input: () => ({
    width: '100%',
    marginBottom: 24,
  }),
  textHelper: () => ({
    position: 'absolute',
    bottom: '-23px',
  }),
  tooltip: (theme) => ({
    fontSize: 14,
    fontWeight: 300,
    lineHeight: '18px',
    color: theme.palette.text.primary,
  }),
};
