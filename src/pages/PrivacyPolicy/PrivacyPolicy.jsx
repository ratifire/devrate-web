import React from 'react';
import { Typography } from '@mui/material';
import { styles } from './PrivacyPolicy.styles';

const PrivacyPolicy = () => {
  return <Typography sx={styles.title}>Privacy Policy Page</Typography>;
};

export default PrivacyPolicy;
