export const styles = {
    contentWrapper: (theme) => ({
width: '480px', 
height:'201px', 
padding: theme.spacing(4),
    }),

title: (theme) => ({
    marginBottom: theme.spacing(2)
}),

description: (theme) => ({
    marginBottom: theme.spacing(3)
}),

buttonGroup: {
width: '432px',
height:'48px',
},
button: {
    border: 'none'
}
}